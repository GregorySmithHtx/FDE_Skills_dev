[project]
name = "fde-skills-dev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "GregorySmithHTX", email = "gregory.terrell.smith@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[project.scripts]
fde-skills-dev = "fde_skills_dev:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
]
