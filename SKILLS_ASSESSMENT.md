# FDE Skills Assessment — Greg Smith

Last updated: 2026-08-29

## Context

"FDE" here means Forward Deployed Engineer in the general sense used across
Palantir, Anduril, Applied Intuition, OpenAI Solutions/Forward Deployed teams,
and similar orgs: an engineer embedded with a client who builds real,
production-grade software against messy real-world data and systems,
end-to-end, under ambiguity, with heavy client interaction.

**Background this assessment is based on:** 10 years as a customer-facing
Solutions Engineer/Architect on Skuid/Nintex Apps (low-code, Salesforce
ecosystem). Project lead on production engagements — not POCs — for
Fortune 500, DOD, and other orgs, often with global teams. Prior to that,
front-end (JS) and Python/Django experience, now ~10 years stale.
Currently exploring whether FDE is the right fit, not actively applying.

## The 9 FDE competency pillars

| # | Pillar | Level | Evidence |
|---|--------|-------|----------|
| 1 | Client engagement & requirements discovery | **Strong** | 10 years doing this professionally, at the project-lead level |
| 2 | Ownership & navigating ambiguity to production | **Strong** | Ships production systems, not demos, for Fortune 500/DOD |
| 3 | Enterprise domain & data literacy | **Strong** | Deep Salesforce data-model fluency; exposure to DOD/enterprise compliance constraints |
| 4 | Full-stack software engineering (hand-written, no low-code scaffold) | **Weak / rusty, real progress** | JS + Python/Django are ~10 years stale; day-to-day work is low-code, so coding reps are low. Calibrated 2026-08-17: can read/understand and adapt existing code, but hasn't written from scratch in ~10 years and reports real blank-page paralysis without a template — coaching should start from a working example to modify, not a blank editor. First guided rep 2026-08-17: extended `paren_triage.py`'s CLI (a `--source` filter) from the existing `--bucket` pattern as a template, plus a real debugging-loop improvement mid-session (chose to run the code and read the traceback rather than request another hint). Second rep 2026-08-18, genuine fundamentals this time: dict/list/tuple distinctions, dict iteration semantics, first real VS Code debugger + Debug Console use, list comprehensions, `any()`. That session's `--bucketXsource`/`--sourceXbucket` filters were left buggy/unfinished at the time, but **were finished and merged shortly after** (`b139d74`, PR #1), with `--verify-source` added on top since (`37826f7`) — the "unfinished" note was stale as of 2026-08-20, corrected here. Third rep 2026-08-19 (`find_person_duplicates.py`, `26e37e8`): extended a working template again (`find_surname_groups` → `find_name_groups`), but this time the design call was his own -- a shared function parameterized by which name-token index to group on, rather than duplicating the fetch/tokenize/group-filter logic per direction -- and he self-caught two real bugs across coached review passes (a stray unused `torch` import, an overcomplicated `argparse` flag simplified to `action="store_true"`) with hints only, no code supplied. Verified working by actually running it against the live DB in both modes. **2026-08-28 (sessions 15-16), first genuine JS/TS reps** — closure/scoping (`var` vs. `let` in a loop, the pre-ES6 IIFE workaround recalled unprompted), destructuring (object/nested/parameter/array-swap, including a self-found constructor argument-order bug and a real default-value misconception caught by testing the edge case rather than being told), and promises/async-await (a full `Promise`/`setTimeout`/`.then()`/`.catch()`/`async-await`/`Promise.all` build with several genuine self-found bugs — an implicit global, a mis-bound `reject` parameter, resolving with an `Error` object instead of calling `reject`, an unhandled-rejection crash). All from-scratch, hints-only, no code supplied — see `PROGRESS_LOG.md` 2026-08-28 for full detail. **2026-08-29 (session 18), Python refresh, closing Phase 0 entirely**: a real `uv` project built from scratch (`uv init`/`uv add`/`uv run`), correctly reasoning out unprompted why `uv.lock` (not `.venv/`) belongs in git, and a genuine mypy-driven catch — a `default=None` argparse field silently defeated a `str`-typed function's type checking (argparse's `Namespace` types as `Any`), self-fixed once with a `str | None` guard, self-flagged as maybe not graceful, then correctly reasoned to the better fix (`required=True` at the CLI boundary, dropping the `Optional` complexity entirely) — same shape as the 2026-08-28 JS `Promise.all` arc, landing the idiomatic answer through his own dissatisfaction rather than being told. Closed with real packaging end-to-end: moved logic into the actual package scaffold, `uv build`, `uv tool install`, a standalone CLI command verified running. Full detail: `PROGRESS_LOG.md` 2026-08-29 |. **2026-08-30/31 (session 19), first SQLAlchemy reps, Phase 1**: wrote `Term`/`Source` ORM models off one worked example, two real self-written bugs (wrong import path for `Enum`, `true`/`True` case), both caught in review rather than self-caught pre-run — tool is brand new to him, expect this to shift with reps the way JS/TS did. Correctly reasoned through a nullability/uniqueness design question once raised and updated the live DDL to match rather than leaving model and table disagreeing. Full detail: `PROGRESS_LOG.md` 2026-08-30/31. **2026-08-31 (session 20)**: continued reps writing `RelationChainMember`/`RelationChain`/`Evidence`/`TimelineItem` himself, real specific bugs each round (missing `primary_key=True` on a composite key, a stray column duplicating a junction table's job, DDL/ORM nullability mismatches) — all caught in review, not yet self-caught pre-run, same "tool still new" arc. Full detail: `PROGRESS_LOG.md` 2026-08-31 (session 20). **2026-09-01 (session 21), first FastAPI reps: genuine first-exposure difficulty with a framework never touched before** — a stubbed app that structurally couldn't run, explicit "I don't know any of this at all" pushback on Claude's teaching pace requiring two corrections in one exchange. Worth keeping distinct from the 2026-08-17 "blank-page paralysis" note (Greg's own correction, 2026-09-01): that note described a more general difficulty starting from scratch even in familiar territory, not day-one unfamiliarity with a brand-new tool — struggling on the first-ever rep of something you've never used is ordinary, not evidence of the same pattern. **2026-09-01 (session 22), same-day continuation: the gap closed noticeably faster than session 21's pace suggested**, consistent with "new tool, first exposure" rather than a deeper block. A full search/create/dedup flow built against live Postgres + SQLite, most self-written bugs self-diagnosed correctly on the first explanation (param-binding tuple vs. bare string, `?` as literal SQL text not an f-string expression, shell vs. Python tilde-expansion, `Enum` member vs. raw-string comparison, a name-shadowing bug causing genuine infinite recursion, traced back once the call-time-vs-definition-time resolution mechanism was explained). Full detail: `PROGRESS_LOG.md` 2026-09-01 (session 22). **2026-09-02 (session 23): first genuine hands-on Alembic rep** — the 2026-08-31 pillar-9 correction ("let me do new things at least once") closing the loop on the exact tool it was about. Didn't recall the mechanics, asked directly, then ran edit-model → `autogenerate` → review diff → `upgrade head` himself end-to-end on a real schema change (dropping `timeline_items.evidence_id`), including self-diagnosing a `script_location` error (wrong working directory) and verifying the result live via `psql`. Full detail: `PROGRESS_LOG.md` 2026-09-02 (session 23)
| 5 | Data engineering & integration (ETL, APIs, messy real data) | **Partial, real data point added** | Some exposure via Salesforce integrations; not deep pipeline/ETL/data-wrangling work. 2026-08-19: directed a long real data-quality-pipeline problem on symbol_constellation (batch dedup, collision detection, orphan/verification checks across hundreds of writes on messy extracted text data) and, from it, correctly specified what a real ingestion pipeline needs — a staging area gated by automated checks before promotion to production — while also right-sizing it down from an illustrative full-enterprise-pipeline comparison (his own framing, later flagged as hyperbole) to what a two-person project actually needs. Real pipeline-architecture judgment; still not hands-on ETL code-writing by him, so not yet moving off Partial |
| 6 | Systems & architecture design for arbitrary domains | **Strong, one specific gap left** | Architects within a bounded low-code platform; hasn't designed a system from a blank canvas recently. 2026-08-17: real unprompted catch on symbol_constellation — flagged that a proposed typed schema table would regress a core multi-source design principle, and proposed the fix (model "deck" as a first-class object with an ordered FK sequence rather than a column). 2026-08-18: a full multi-turn design session for the same idea — independently spotted a missing base object in his own hierarchy sketch, used real physical decks to stress-test the model (surfacing a genuinely hard edge case, not a convenient one), and self-corrected an over-loaded table design into normalized subtype tables. 2026-08-19: the Deck/Card/MajorArcanaCard/SuitCard/ExtraCard schema (`38fddba`) is confirmed live and populated (7 decks, 210 `Card` rows) — no longer design-only. Same session, separately: rejected reusing `member_of` for a new relationship because he could see in advance it would blur three already-distinct uses of it, and specified unprompted that a data-quality fix belonged in the display layer (derive state from structure at render time, don't store it redundantly) rather than the data layer. **Raising to Strong** — repeated, independent, correct architectural judgment across multiple sessions and problem shapes is the actual bar. 2026-08-19 (later session): a new facet of the same pillar — pipeline/process architecture rather than data-schema — floated a staged-promotion ingestion design for a future hard source, illustrated it with a full enterprise-style release pipeline, then correctly scoped that comparison down to what a two-person project's actual risk profile needs (and named his own illustration as hyperbole rather than let it stick as the plan) rather than defaulting to enterprise ceremony. What's still missing before this is unqualified: he directs and reviews this work, he doesn't yet build it hands-on — see pillar 4, which this doesn't move. **2026-08-30/31 (session 19, `theutus-db-timeline-phase`, Phase 1)**: a full schema-design arc for a new Postgres domain, several rounds of unprompted, correct judgment — reversed an early naming choice once it stopped earning its keep, caught a plural/singular naming inconsistency before rebuilding the DB, simplified his own design by recognizing a source/work 1:1 relationship didn't need a separate table, and flagged a legacy column (`terms.source_id`) as semantically wrong to migrate despite looking easy to. Sharpest instance: caught a design flaw in **Claude's own** suggested fix (dropping `sources.title` in favor of a term-name lookup) by connecting it to an already-established project rule (the parens-ban "glossary test"), then derived the correct split (full title vs. glossary-safe name) himself. Full write-up: `PROGRESS_LOG.md` 2026-08-30/31. **2026-08-31 (session 20), same domain, the deepest arc yet — independent generalization across multiple rounds, not isolated catches.** Spotted a foreign-key cycle in his own proposed design before building it; asked for a real industry paradigm ("it seems like Wikipedia has this down, but I don't know how it does it") rather than continuing to invent rules piecemeal; once shown Wikidata's actual model, generalized it himself into a domain/range-style constraint idea and a general satellite-modeling rule, then applied that rule unprompted to catch two violations sitting in his own already-built design (`sources.author_person_id`, `sources.sub_system_id`); independently identified the real risk behind a proposed uniqueness constraint (competing sources disagreeing about one edge) before being shown the resolution. Full write-up: `PROGRESS_LOG.md` 2026-08-31 (session 20). **2026-09-01 (session 22): first instance of the design-and-build gap narrowing.** Specified a real dedup rule for application logic (not schema) himself — no legacy match → create; legacy match → import only what's missing — then built and verified it hands-on against live data. Still one data point in application-logic territory specifically, not enough alone to move the rating, but directly relevant to the "directs/reviews, doesn't yet build hands-on" caveat below. Full write-up: `PROGRESS_LOG.md` 2026-09-01 (session 22). **2026-09-02 (session 23): another unprompted schema-design catch, plus a real translation of known-domain expertise into a service-layer architecture preference.** Caught, before it was built, that a proposed `TimelineItem` route was structurally wrong (needed a 3-table fan-out through `Evidence`, not a single insert), then independently simplified the schema itself — reasoned `TimelineItem` doesn't need its own `evidence_id` at all, since the event it points at is already a term with its own evidence, and generalized the disputed-date edge case to the schema's existing base/sibling-term pattern rather than inventing new multiplicity. Separately, diagnosed the real problem with the growing `search_legacy_X_by_Y` helper tangle (search and create fused into one function each) and translated a known tool from his own background (Skuid's model-driven forms — search/existence-check in the browser, assemble a multi-part object, submit once viable) into a stated architecture preference, explicitly rejecting Claude's first suggestion (ORM event hooks/implicit triggers) as not matching it. Full write-up: `PROGRESS_LOG.md` 2026-09-02 (session 23) |
| 7 | Engineering rigor (git, testing, CI/CD, security-by-construction) | **Weak / unknown** | Low-code platforms absorb most of this; needs a direct check |
| 8 | Modern AI/LLM application fluency (building with LLMs — RAG, tool use, agents) | **Developing / real prompt-and-tool-use authorship on a working system, orchestration code still ahead** | Not assessed before 2026-08-24. Session 10: directed a real multi-agent LLM pipeline in production on `symbol_constellation` — parallel background agent dispatch, live mid-task guidance relayed into a running agent via message-passing, diagnosing and fixing a subagent's system-prompt reasoning gap. Substantive exposure to how an agent system actually behaves and fails, but Claude wrote every agent definition and orchestration call — direction/review only. **Session 11, the real move: hand-authored two coordinated `.claude/agents/*.md` instruction files himself** — a long multi-turn rewrite of `synthesist.md` (relocating a misplaced content-word/proper-name boundary, catching his own broken query design mid-draft, designing an entirely new task — "Task 3, base term usage search" — from scratch) plus a matching edit to `glossarist.md`'s Reporting section so a second agent's output flags when that new task is owed, closing a real cross-agent loop (Glossarist has no `Agent` tool and can't trigger the sweep itself). Corrected here from the original write-up, which credited only the `synthesist.md` half. This is squarely "prompting, tool use/agents" by this pillar's own definition — the prior framing that only orchestration *code* counts was too narrow given what the pillar is actually named. Claude's role was review-only, doing just the final doc-consistency polish once told to go ahead (so the landed commit reflects Claude's formatting pass, not raw authorship — the design and content were his). **Session 12 validated it working in production**: Task 3 correct across 10 new base terms and ~43 relations, and the Glossarist-flags-Task-3-is-owed loop fired correctly on every creation. Also continued directing orchestration at greater complexity (15+ parallel dispatches across dependency-ordered waves, diagnosing a stalled agent's real cause, resuming one mid-task via message-passing) — still Claude's code, not moving this further on its own. **What's still the real gap, keeping this off "Strong"**: the orchestration/tool-calling layer itself (a Historian agent's actual tool-definitions and agent loop, not just its governing prompt) — planned but not yet started, blocked on a schema dependency (`timeline_item`) that doesn't exist yet. Full write-up: `PROGRESS_LOG.md` 2026-08-24 |
| 9 | AI-assisted engineering judgment — catching wrong AI output, architecture-under-uncertainty, tool/process skepticism | **Early, but eight dated same-shaped reps across eight different problem shapes as of 2026-08-28 — candidate for "Developing," not yet moved without review** | **2026-08-28 (JS promises box, `FDE_Skills_dev`), eighth instance, a new target — Claude's own suggested designs mid-lesson, not a subagent or tool**: sustained pushback across several rounds rather than a single catch. Reopened a design point ("was it really misplaced?") after initially accepting it; independently noticed that a suggested `async/await` + `try/catch` rewrite serialized two lookups that used to run concurrently ("that's way worse than the previous alternative") before being told; rejected the `Promise.all` fix too, for a specific, correct reason (fail-fast semantics lose an already-successful result, plus unwanted `try/catch` ceremony); then **independently derived the actual professional answer himself** — resolve with `object | null` instead of reject/`.catch()`, because "not found" is an expected outcome, not an exceptional one — arriving at it through repeated dissatisfaction with each intermediate version rather than being handed the pattern. Closed with a sharp, honest read of the exercise's own limits (in a single-consumer toy script, the two designs he'd been arguing about are actually observationally identical) rather than either defending the abstraction or concluding the principle itself was wrong. Full write-up: `PROGRESS_LOG.md` 2026-08-28. **2026-08-27/28 (SQL box, `FDE_Skills_dev`), seventh instance, new source domain (own SQL work, not `symbol_constellation`) and a new target (the terminal/display layer itself)**: two catches in one session. First, found a genuine aggregate fan-out bug in his own query (a double join off the same key silently multiplying a `COUNT`) not by it erroring, but by noticing `evidence_count` was a suspiciously clean multiple of `relation_count` across every row, then verifying one case against ground truth before trusting the fix. Second, sharper: after a subquery result looked short, didn't accept it — flagged the gap, and it turned out every pasted result that session had been silently truncated by psql's own pager, not the SQL. Same shape as the 2026-08-21 "grep-the-code can't see a hand-run DB command" catch, extended to a new target: the tool you're looking at the output *through*, not just the query or the AI. Full write-up: `PROGRESS_LOG.md` 2026-08-27/28. `symbol_constellation` project (see below) is generating real, non-hypothetical reps of this. 2026-08-17: caught a real, subtle bug in Claude's own suggested one-line fix (a Python loop-variable-scoping leak that would have silently printed the wrong value, no crash) before applying it, by correctly reasoning through the scoping rule rather than pasting the suggestion as-is. 2026-08-19: catching that a *verification tool's own heuristic* (`paren_triage.py`'s `collision_ok` bucket) was being trusted as a final answer when it couldn't actually distinguish the case it claimed to resolve — named the general principle himself: a tool's "ok" label is an approximation, not a verified answer. 2026-08-23, one level more abstract: diagnosed a *systemic* governance failure, not a single bad output — a rule fully documented in Claude's own memory system still got violated at scale (1,556/2,586 terms) because memory-based instruction-following is inherently probabilistic per-decision, not a hard constraint. Unprompted, proposed the actual fix at the right level: narrow, single-purpose agents whose entire context is their fixed system prompt (loaded in full, every time) rather than a general session's fallible memory recall. **2026-08-24, a different and arguably harder move than any prior entry — debugging, not designing**: when a Synthesist subagent declined a plainly-correct connection (reasoning no source passage backed it), refused Claude's offer to just patch that one instance ("I don't want you to fix them, we have to fix the Synthesist. What would preclude the agent from being able to make this connection?") and demanded the actual mechanism be found and fixed at the instruction-file level — his diagnostic instinct (a missing dictionary-verification step) was confirmed exactly right once root-caused. Same session, also caught a real bug in *Claude's own* answer (not a subagent's) by refusing to accept "seems safe" and specifying the actual verification criterion himself. **2026-08-24 (session 11), a self-catch this time, mid-draft rather than mid-review**: while hand-writing a new step for `synthesist.md`, caught his own design flaw before Claude did — a relation-existence-check query referencing a `term_id` that couldn't exist yet, since the base term it would point to hadn't been created. Same rigor now applied to his own output, not just Claude's or a subagent's. **2026-08-24 (session 12), the 2026-08-23 pattern repeated on a new bug — now a demonstrated habit, not a one-off**: a subagent correctly built a needed base term to complete its own task, but declined an identical structural gap found elsewhere in the same task, flagging it "out of scope" instead. Refused to let the inconsistency stand: "It's a genuine miss that it came to this conclusion and didn't create a base... Where do we need another base creation entry point?" — the same demand for the systemic instruction-file cause, not a patch, applied to a second, different subagent bug eight days later. Also separately reviewed a flagged relation-type ambiguity (`instance_of` vs. `derivation_of`) on its own merits and made the call himself rather than defaulting to the agent's or Claude's framing. **2026-08-25 (session 13), sixth instance, new problem shape (data integrity, not naming/query-design/base-term-creation)**: refused Claude's "this is fine, it's just historical preservation" read of superseded terms still carrying evidence rows, asking instead "did they move to the active term?" — the sharper question that surfaced a real, systemic bug (evidence being copied, not moved, on ~half the session's supersessions, some 3-way duplicated). The fix that followed is also a genuine production-engineering pattern in its own right: root-caused to a specific instruction-file ambiguity, then fixed at both levels — the instruction file (prevent recurrence) and the existing data (22 rows cleaned up by hand, 3 edge cases explicitly named rather than force-resolved). Full write-up: `PROGRESS_LOG.md` 2026-08-25. **2026-08-31 (session 20), a new problem shape — catching a flaw in the collaboration process itself, not in a piece of AI output.** Claude executed an entire Alembic adoption directly (init, migration authoring, applying it) reasoning it was administrative-looking plumbing, the same bucket as an earlier file-split Claude had also done directly without objection. Greg's correction named the actual distinction precisely: "next time let me do new things at least once" — mechanical-looking isn't the same test as new-to-him, and the file-split precedent didn't transfer just because both looked like plumbing on the surface. This pillar's own 2026-08-16 note flagged that corrections "tend to land late... rather than on first repeat"; this one landed the same session, immediately after the pattern completed — the fastest turnaround of any instance logged here. Full write-up: `PROGRESS_LOG.md` 2026-08-31 (session 20) |

### Why pillar 9 exists

As of 2026, most literal code-writing on FDE-style work gets delegated to
AI. That shifts where the actual skill lives: it's not "can you write the
code," it's domain judgment — catching subtly-wrong AI output, making
architecture calls under uncertainty, and closing the gap between what's
asked for, what's meant, and what actually gets built. This is arguably
becoming the *central* FDE skill, not a side one. It's distinct from pillar
8 (building LLM-powered apps) — this is about working correctly alongside
an AI collaborator on any codebase, which is now true of nearly all FDE
work regardless of stack.

## What you already have (don't relearn this)

- **Client trust and requirements translation.** The hardest part of FDE work
  for most engineers — turning an ambiguous stakeholder ask into a scoped
  build — is already a professional strength.
- **Production accountability.** You own outcomes for Fortune 500 and DOD
  clients, including the political/organizational complexity of global
  teams. Most engineers learning to code have never carried this.
- **Enterprise data/security intuition.** Working inside Salesforce and DOD
  constraints has given you real instincts about data governance, access
  control, and integration boundaries — this transfers directly.

## Where you need improvement (rust, not gaps)

- **Modern JavaScript/TypeScript.** ES6+, async patterns, a modern framework
  (React is the default choice for FDE-style rapid UI work). **In progress
  2026-08-28**: `let`/`const`/closures, destructuring, and promises/async-await
  covered with real self-directed reps (see pillar 4 and `PROGRESS_LOG.md`
  2026-08-28); modules and TypeScript's type system still open.
- ~~Python, current tooling basics.~~ **Closed out 2026-08-29** — real `uv`
  project mechanics (venvs, dependency management, dev-dependencies,
  packaging/`uv build`/`uv tool install`), type hints verified with `mypy`
  (including a genuine caught-then-fixed design bug, not just annotation
  syntax), and f-strings. See `PROGRESS_LOG.md` 2026-08-29. **Still open**:
  a modern web framework (FastAPI over Django for FDE-style fast, API-first
  builds) — deferred to Phase 1's full-stack rebuild, not part of the
  fundamentals refresh.
- **Git fluency.** Branching, rebasing, resolving conflicts, PR workflow —
  worth a direct honest check since low-code tools often hide this.
- ~~SQL beyond a platform's abstraction layer.~~ **Closed out 2026-08-27/28**
  — real Postgres work against a live dataset (`fde_practice`), self-driven:
  joins, aggregates, indexes (2026-08-21), `HAVING`, and subqueries/window
  functions (2026-08-27/28), including finding and fixing a genuine
  aggregate fan-out bug (a double join off the same key silently multiplying
  a `COUNT`) by reasoning from the output's own numeric pattern rather than
  trusting a query that ran without erroring. `LEARNING_ROADMAP.md` Phase 0
  SQL box checked off. See `PROGRESS_LOG.md` 2026-08-27/28 for detail.

## What's a true gap — learn from the basics

- **Data engineering.** Ingesting messy, real-world (non-Salesforce-shaped)
  data; ETL/ELT patterns; schema design for a domain that doesn't already
  have Salesforce's object model imposed on it.
- **Systems design from a blank canvas.** Architecting a full-stack app's
  layers (frontend, API, data store, auth) yourself, including the tradeoffs
  a low-code platform normally makes for you.
- **Engineering rigor as a practice.** Automated tests, CI/CD, containerized
  deploys, basic cloud hosting (AWS/GCP/Azure) — these are rarely required
  inside a low-code platform and are core to FDE credibility.
- **Security fundamentals for code you write**, as opposed to configuring a
  platform's built-in security model (auth flows, secrets handling, input
  validation, least-privilege API design).
- **Modern AI/LLM application building** — prompting, tool use/agents, RAG
  basics. Not traditionally "FDE," but increasingly load-bearing at the
  companies hiring for this role today.

## Active practice: symbol_constellation

A separate personal project (extracting/structuring data from historical
texts into a graph database, with report UI and audit tooling) is
currently the live vehicle for pillar 9 and parts of 5–7. Concrete, dated
evidence from this project belongs in `PROGRESS_LOG.md` as it accumulates.
First entry: 2026-08-16, see log for specifics (trust-calibration patterns
with AI-generated code, git-discipline recovery across parallel sessions,
triage discipline for deferred work).

## Bottom line

The client-facing and ownership pillars — usually the bottleneck for people
trying to break into FDE roles — are already there. The gap is concentrated
and known: hands-on, hand-written software engineering outside a low-code
platform's guardrails. That's a tractable, well-scoped relearning project,
not a career pivot from zero. See `LEARNING_ROADMAP.md` for the plan.
